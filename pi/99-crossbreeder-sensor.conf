# NetworkManager must keep its hands off the radio the sensor tests with:
# two supplicants on one interface fight, and the symptom is an association
# that works and then drops thirty seconds later.
#
# Change the interface name here if the sensor's test radio is not wlan1.
[keyfile]
unmanaged-devices=interface-name:wlan1
